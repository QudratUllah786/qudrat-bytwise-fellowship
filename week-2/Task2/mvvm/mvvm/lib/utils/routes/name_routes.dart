class RoutesName{
  /// login screen route name
  static const String loginScreen = '/'; /// we use backSlash(/) when we use Getx
  /// home screen route name
  static const String homeScreen = '/home_screen';  /// we use backSlash(/) when we use Getx
}